## Testing static MDX render

This is some mdx content

> With a blockquote

~~strikethrough~~

<fileContext.Consumer>
  {({ files }) => (
    <ul>
      {files.map(f => (
        <li key={f}>{f}</li>
      ))}
    </ul>
  )}
</fileContext.Consumer>
