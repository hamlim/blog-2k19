import Header from '@components/header.js'
import UseLazyRef from './use-lazy-ref.mdx'
import Wrapper from '@components/HookRecipesWrapper.js';

<Header />

# React Hook Recipes

This page is meant to be a collection of common custom React hooks.

> If you don't know what React hooks are yet, make sure to check out their docs:
> [here](http://reactjs.org/hooks)

### List:

- `useLazyRef` - Lazily initialize a ref variable

<UseLazyRef />



export default Wrapper